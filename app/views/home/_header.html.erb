<nav class="flex justify-between items-center w-full py-3 px-4 bg-white shadow-md fixed top-0 z-50">
  <!-- Left Section: Logo -->
  <div class="flex items-center gap-4 w-[180px]">
    <img src="<%= asset_path('icon.svg')%>" class="w-6 md:w-8" alt="icon">
    <p class="text-xl md:text-3xl font-serif font-semibold text-gray-800">Mazee</p>
  </div>

  <!-- Right Section: Search Bar -->
  <div class="hidden md:flex gap-6 p-2 items-center w-2/3 border-2 rounded-2xl" style="border-color: #3333334D;">
    <img src="<%= asset_path('search.svg')%>" class="w-6 h-6" alt="search">
    <input class="w-full text-lg font-medium text-gray-600 focus:outline-none" placeholder="Search here...">
  </div>

  <!-- Search Icon for Mobile -->
  <div class="md:hidden p-2">
    <img src="<%= asset_path('search.svg')%>" class="w-6 h-6" alt="search">
  </div>
<!-- Sidebar Navigation -->
<div class="flex flex-col w-[180px] h-screen bg-white shadow-md fixed left-0 top-[56px]">
  <%= link_to "Feed", home_path, class: "w-full px-4 py-3 rounded-lg text-gray-800 hover:bg-gray-200 text-lg font-medium #{current_page?(home_path) ? 'bg-[#377DFF] text-white' : ''}" %>

  <% if current_user.has_role?(:admin) %>
<%= link_to "Manage Users", admin_root_path, class: "w-full px-4 py-3 rounded-lg text-gray-800 hover:bg-gray-200 text-lg font-medium #{current_page?(admin_root_path) ? 'bg-[#377DFF] text-white' : ''}" %>
    <%= link_to "Report", admin_reports_path, class: "w-full px-4 py-3 rounded-lg text-gray-800 hover:bg-gray-200 text-lg font-medium #{current_page?(admin_reports_path) ? 'bg-[#377DFF] text-white' : ''}" %>
  <% end %>

  <% if user_signed_in? %>
    <div class="p-2 w-full">
      <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "w-full px-4 py-3 rounded-lg bg-red-500 text-white hover:bg-red-600 text-center text-lg font-medium" %>
    </div>
  <% end %>
</div>
</nav>
